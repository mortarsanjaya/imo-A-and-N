import IMO2020.N5.additive_map.regular_map IMO2020.N5.additive_map.sporadic_map

/-!
# Distinction between `p`-regular and sporadic additive maps

Fix a commutative additive monoid `M` and a prime `p`.
Let `f` be a `p`-regular additive map.
We prove that `f` must be zero if either
* `f` is sporadic, or
* `f` is `q`-regular for some prime `q ≠ p`.
-/

namespace IMOSL
namespace IMO2020N5

end IMO2020N5
end IMOSL
